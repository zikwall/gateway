name: build_and_tests
on:
  push:
    paths-ignore:
      - 'bin/**'
      - '.github/**'
      - '**.md'
      - '.gitignore'
      - 'example/**'
    tags:
      - v*
    branches:
      - master
      - main
      - feature/**
      - bugfix/**
  pull_request:
    types:
      - opened
jobs:
  build:
    name: Build
    strategy:
      matrix:
        go-version: [1.19.x]
        platform: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go-version }}

      - name: Check out source code
        uses: actions/checkout@v2

      - name: Build
        env:
          GOPROXY: "https://proxy.golang.org"
        run: go build ./example/simple/cmd/gateway/main.go

      - name: Test
        env:
          GOPROXY: "https://proxy.golang.org"
        run: go test -v ./...